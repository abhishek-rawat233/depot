# json.extract! product, :id, :title, :description, :image_url, :price, :created_at, :updated_at, :category_id
json.extract! product, :title, :category_id
# json.url product_url(product, format: :json)
